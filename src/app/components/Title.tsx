import Image from 'next/image'

const Title = () => {
  return (
    <div className="sticky top-0 z-50 flex justify-center items-center w-full p-12 mb-0 px-6 bg-black">
      <h1 className="absolute left-1/2 transform -translate-x-1/2 text-3xl text-white font-semibold tracking-wider">
        Chai
      </h1>
     </div>
  );
};

export default Title; 